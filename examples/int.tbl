extern task putchar(c: u32);

task add(a: u32, b: u32) -> u32 {
    return a + b;
}

task main() -> u32 {
    var a: u32 = 1;
    var b: u32 = 2;
    var c: u32 = add(a, b);
    putchar(48 + c);
    return 0;
}