extern task putchar(n: u32);

task fib(n: u32) -> u32 {
    if (n == 0) || (n == 1) {
        return 1;
    } else {
        return (fib(n - 1)) * (fib(n - 2));
    }
}

task main() -> u32 {
    var n: u32 = 5;
    var m: u32 = fib(n);
    return m;
}